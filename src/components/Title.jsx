import React from 'react';
import styled from 'styled-components';

const TitleText = styled.div`
  font-family: Aileron;
  font-weight: bold;
  font-size: 24px;
  color: #4271bc;
  text-align: center;
`;

const Title = () => <TitleText>Intercom REST API</TitleText>;

export default Title;
